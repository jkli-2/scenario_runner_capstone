[project]
name = "scenario-runner-0-9-15"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "py-trees (==0.8.3)",
    "numpy (==1.18.4)",
    "networkx (==2.2)",
    "shapely (==1.7.1)",
    "psutil (>=7.0.0,<8.0.0)",
    "xmlschema (==1.0.18)",
    "ephem (>=4.2,<5.0)",
    "tabulate (>=0.9.0,<0.10.0)",
    "opencv-python (==4.2.0.32)",
    "matplotlib (<3.5)",
    "six (>=1.17.0,<2.0.0)",
    "simple-watchdog-timer (>=0.1.1,<0.2.0)",
    "antlr4-python3-runtime (==4.10)",
    "graphviz (>=0.20.3,<0.21.0)",
    "carla (>=0.9.15,<0.10.0)"
]

[tool.poetry]
package-mode = false


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
